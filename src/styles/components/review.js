import config from '@src/config/css'
export const reviewStyle = {
  position: config.FIXED,
  top: 0,
  bottom: 0,
  left: 0,
  right: 0,
  height: '100%',
  paddingVertical: 36,
  backgroundColor: '#999',
  justifyContent: 'center',
  alignItems: 'center'
}
